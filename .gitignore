# Segredos locais — nunca versionar (regra 1 do CLAUDE.md)
.dev.vars
.dev.vars.*
!.dev.vars.example
.env
.env.*

# Dependências e artefatos de build
node_modules/
.wrangler/

# Gerado por scripts/csv-para-seed.js — regenerar a partir do CSV
seed.sql

# Sistema operacional
.DS_Store
Thumbs.db

# Gerado no build a partir de brand/tokens.css (scripts/sincroniza-tokens.js)
public/tokens.css

# Gerado no build a partir de data/versao-acervo.txt (scripts/sincroniza-tokens.js)
public/versao-acervo.js
