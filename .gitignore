# Segredos locais — nunca versionar (regra 1 do CLAUDE.md)
.dev.vars
.dev.vars.*
!.dev.vars.example
.env
.env.*

# Dependências e artefatos de build
node_modules/
.wrangler/

# Sistema operacional
.DS_Store
Thumbs.db

# Gerado no build a partir de brand/tokens.css (scripts/sincroniza-tokens.js)
public/tokens.css

# Gerado no build a partir de dados/vocabulario.json (scripts/sincroniza-tokens.js)
public/vocabulario.js

# Telas geradas no build a partir de paginas/ e parciais/ (scripts/gera-paginas.js)
public/index.html
public/resultado.html
public/sobre.html
public/privacidade.html
public/caminhos/
public/estilos.css
public/configuracao.js
public/_redirects
public/assets/

# Painel de edição, gerado no build (etapa 9): a tela, o estilo e o script de
# tela que o build copia de scripts/admin/
public/admin.html
public/admin.css
public/admin/

# Gerados no build para o painel conferir cor e estado do deploy (etapa 9)
public/paleta.json
public/versao-build.json
